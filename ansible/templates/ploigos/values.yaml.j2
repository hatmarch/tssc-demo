# deploy a tssc platform to these namespaces
operator:
  namespaces:
  - name: {{ ploigos_namespace }}
    version: {{ ploigos_operator_version }}
    channel: alpha
    operatorname: ploigos-software-factory-operator
    operatorgroup:
      create: true
